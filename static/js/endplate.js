// Upload button functionality (to make sure the user gets the file)
/*$('#upload').click(function (e) {
	e.preventDefault();

	link = $('#logfile').attr('href');
	window.open(link);
    window.location.href = '/upload';
});*/